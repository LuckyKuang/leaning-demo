server:
    port: 9009
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: root
        password: 123456
        url: jdbc:mysql://localhost:3306/learning-demo?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8&autoReconnect=true&rewriteBatchedStatements=true
    servlet:
        multipart:
            # 单个文件最大值
            max-file-size: 100MB
            # 单次请求最大值
            max-request-size: 100MB
